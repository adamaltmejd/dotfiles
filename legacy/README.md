# Legacy

This directory is a migration holding area for old dotfile layouts.

- `legacy/server-config/` contains the archived pre-bootstrap setup.
- Content under `legacy/server-config/` is not part of active runtime configuration.
- Keep only as long as needed for migration confidence, then remove.
