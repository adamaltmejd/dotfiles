# Egress allowlist for the agentbox container. One domain per line; squid
# dstdomain syntax, so a leading dot matches the domain and all subdomains
# (.opencode.ai covers opencode.ai and api.opencode.ai) while a bare name
# matches exactly. Lines starting with # are ignored.
#
# Everything not listed here is refused with 403, and the container is on a
# host-only network, so there is no route around this file.
#
# Keep it as small as the task allows: every entry is somewhere the agent
# could send your source code.

# opencode Zen / Go -- the model endpoint. The reason this box exists.
.opencode.ai

# Package registries, for installing project dependencies inside /workspace.
.npmjs.org
.pypi.org
.pythonhosted.org
.crates.io
cloud.r-project.org

# Debian archives, for apt during image build and inside a session.
deb.debian.org
security.debian.org

# Public GitHub reads: cloning dependencies, fetching release tarballs. No
# git credentials are forwarded into the container, so this is read-only
# access to public repositories. Delete these lines for a stricter box.
github.com
codeload.github.com
.githubusercontent.com
