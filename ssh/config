# Global SSH configuration
# Included by ~/.ssh/config

# Connection multiplexing
ControlMaster auto
ControlPath ~/.ssh/sockets/%r@%h:%p
ControlPersist 1800

# Default settings for all hosts
Host *
    AddKeysToAgent yes
    UseKeychain yes
    IdentitiesOnly yes
    ServerAliveInterval 60

# Additional hosts configurations in config.d
Include /Users/adam/.config/ssh/config.d/*