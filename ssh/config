# Global SSH configuration
# Included by ~/.ssh/config

# Connection multiplexing
ControlMaster auto
ControlPath ~/.ssh/sockets/%C
ControlPersist 1800

# Default settings for all hosts
Host *
    AddKeysToAgent yes
    UseKeychain yes
    IdentitiesOnly yes
    ServerAliveInterval 60

# Additional hosts configurations in config.d
Include ~/.config/ssh/config.d/*
