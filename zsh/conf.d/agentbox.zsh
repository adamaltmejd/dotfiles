#!/usr/bin/env zsh
# Sandboxed coding agent (pi in an Apple container). Its own repo, cloned
# separately: https://github.com/adamaltmejd/agentbox

[[ -x "$HOME/Code/agentbox/agentbox.sh" ]] && alias agentbox="$HOME/Code/agentbox/agentbox.sh"
